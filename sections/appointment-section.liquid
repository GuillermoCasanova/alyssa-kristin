{{ 'section-appointments.css' | asset_url | stylesheet_tag }}

<section>
  <div class="appointment-section">
    <div class="appointment-section__inner">
      <div class="">
        <div class="appointment-section__image">
          <div class="apppointment-section__image__innner">
            {% if section.settings.image_mobile != blank %}
              <img
                srcset="
                  {%- if section.settings.image_mobile.width >= 165 -%}{{ section.settings.image_mobile | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if section.settings.image_mobile.width >= 360 -%}{{ section.settings.image_mobile | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if section.settings.image_mobile.width >= 533 -%}{{ section.settings.image_mobile | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if section.settings.image_mobile.width >= 720 -%}{{ section.settings.image_mobile | image_url: width: 720 }} 720w,{%- endif -%}
                  {{ section.settings.image_mobile | image_url }} {{ section.settings.image_mobile.width }}w
                "
                src="{{ section.settings.image_mobile | image_url: width: 533 }}"
                sizes="(min-width: 940px) 30vw, 50vw"
                alt="{{ section.settings.image_alt | escape }}"
                class="motion-reduce responsive-image hide-for-large-up"
                loading="lazy"
                width="{{ section.settings.image_mobile.width }}"
                height="{{ section.settings.image_mobile.height }}"
              >
            {% endif %}
            {% if section.settings.image_desktop != blank %}
              <img
                srcset="
                  {%- if section.settings.image_desktop.width >= 165 -%}{{ section.settings.image_desktop | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if section.settings.image_desktop.width >= 360 -%}{{ section.settings.image_desktop | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if section.settings.image_desktop.width >= 533 -%}{{ section.settings.image_desktop | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if section.settings.image_desktop.width >= 720 -%}{{ section.settings.image_desktop | image_url: width: 720 }} 720w,{%- endif -%}
                  {{ section.settings.image_desktop | image_url }} {{ section.settings.image_desktop.width }}w
                "
                src="{{ section.settings.image_desktop | image_url: width: 533 }}"
                sizes="(min-width: 940px) 30vw, 50vw"
                alt="{{ section.settings.image_alt | escape }}"
                class="motion-reduce responsive-image show-for-large-up"
                loading="lazy"
                width="{{ section.settings.image_desktop.width }}"
                height="{{ section.settings.image_desktop.height }}"
              >
            {% endif %}
          </div>
        </div>
      </div>
      <div class="appointment-section__copy">
        <div class="page-width appointment-section__copy__inner">
          <h2 class="appointment-section__copy__heading">
            {{ section.settings.headline }}
          </h2>
          <div class="appointment-section__copy__body">
            <p>{{ section.settings.body_copy }}</p>
          </div>
          <div class="appointment-section__copy__cta">
            <a href="{{ section.settings.cta_link }}" class=" button button--medium  button--primary">
              {{- section.settings.cta_text -}}
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Appointment Section",
  "enabled_on": {
    "templates": ["index", "page"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Image Mobile"
    },
    {
      "type": "image_picker",
      "id": "image_desktop",
      "label": "Image Desktop"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image Alt Text"
    },
    {
      "type": "richtext",
      "id": "headline",
      "label": "Headline",
      "default": "<p>Visit Us</p>"
    },
    {
      "type": "richtext",
      "id": "body_copy",
      "label": "Body Copy",
      "default": "<p>Discover your dream dress at our River North Chicago loft studio.445 W Erie St #108, Chicago, IL 60654</p>"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Link"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Text",
      "default": "Book an Appointment"
    }
  ],
  "presets": [
    {
      "name": "Appointment Section"
    }
  ]
}
{% endschema %}
